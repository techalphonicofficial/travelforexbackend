<div class="mb-4">
    <h2 class="fw-bold mb-1">Edit Module: <span class="text-primary"><%= module.name %></span></h2>
    <p class="text-secondary small mb-0">Modify the module name for consistent organization.</p>
</div>

<div class="card shadow-sm border-0 mb-4 p-4 max-w-lg">
    <form action="/modules/<%= module.id %>?_method=PUT" method="POST">
        <div class="mb-3">
            <label for="name" class="form-label fw-semibold text-secondary small">Module Name</label>
            <input type="text" class="form-control p-2 shadow-none border-secondary-subtle" id="name" name="name" value="<%= module.name %>" required>
        </div>

        <div class="d-flex justify-content-end gap-3 pt-3 mt-4 border-top">
            <a href="/modules" class="btn btn-light px-4">Cancel</a>
            <button type="submit" class="btn btn-primary px-4 fw-bold">Update Module</button>
        </div>
    </form>
</div>

<style>
.max-w-lg { max-width: 600px; }
</style>
